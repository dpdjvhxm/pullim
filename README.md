# pullim - 로그인 + 공지사항 게시판 웹서비스

Python 표준 라이브러리(WSGI + SQLite)만으로 만든 기본 로그인/회원가입 및 공지사항 게시판 예제입니다.

## 기능
- 회원가입/로그인/로그아웃
- 로그인 사용자만 공지사항 작성 가능
- 비밀번호는 해시(PBKDF2) 형태로 저장
- 공지사항 목록(작성자/작성시각) 조회

## 실행 방법
```bash
python app.py
```

브라우저에서 `http://localhost:5000` 접속.
